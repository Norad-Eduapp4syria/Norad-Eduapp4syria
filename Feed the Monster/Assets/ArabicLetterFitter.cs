using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class ArabicLetterFitter : MonoBehaviour {

	public Font font;
	// Use this for initialization
	void Start () {

	}

	// Update is called once per frame
	void Update () {
		

	}

	void Test(){
		//Font font = GetComponent<Text> ().cachedTextGenerator;
		//print(GetComponent<Text> ().cachedTextGenerator.verts[0].position);

		/*char chr = GetComponent<Text> ().text [0];
		print (font.characterInfo.Length);

		CharacterInfo chrInfo = font.characterInfo[0];

		print(chr + " : "  + font.GetCharacterInfo (chr, out chrInfo, 32) + "  -  " + chrInfo.glyphHeight );*/
	}
}
